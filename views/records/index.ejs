<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title></title>
        <link rel="stylesheet" href="/css/records.css" />
        <link rel="preconnect" href="https://fonts.gstatic.com">
        <link href="https://fonts.googleapis.com/css2?family=New+Rocker&display=swap" rel="stylesheet"> 
    </head>
    <body>
        <div class="container">
        <h1>Records Index Page</h1>
        <nav>
            <a href="/records/new">Create a New Record</a>
        </nav>
        <ul>
            <% for(let i=0; i < records.length; i++) { %>
                <li>
                    <a href="/records/<%=records[i].id%>"> <%=records[i].title %></a><br>
                    <%=records[i].artist %>

                    <form action="/records/<%=records[i].id%>?_method=DELETE" method="POST">
                        <a href="/records/<%=records[i].id%>/edit">EDIT</a><br>
                        <input type="submit" value="DELETE" /><br>
                    </form><br>
                </li>
            <% } %>
        </ul>
        </div>
    </body>
</html>